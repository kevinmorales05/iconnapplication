const config = {
    screens: {
        AuthStack: {
            screens: {
                ContinueWith: 'continuewith'
            }
        }
    }
};
const linking = {
    prefixes: ['iconn://iconn/'],
    config,
};
export default linking;

